#ifndef gamemodes
#define gamemodes
#include <ncurses.h>

extern int modeChooser(int mode, WINDOW* window, int window_height, int window_width, 
                       int window_startx, int window_starty);

#endif
